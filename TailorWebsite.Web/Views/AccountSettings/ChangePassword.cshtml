@model TailorWebsite.ViewModels.VM.ChangePasswordViewModel
@{
    ViewData["Title"] = "Zmiana hasła";
}

<section class="position-relative overflow-hidden rounded-4 text-white dark-blue mb-4 shadow p-4">
    <div class="row align-items-center">
        <div class="col-lg-8">
            <h5 class="text-warning text-uppercase letter-spacing-2 mb-2">Bezpieczeństwo</h5>
            <h1 class="display-5 fw-bold mb-2">Zmiana hasła</h1>
            <p class="text-white-50 mb-0">Wybierz mocne hasło, aby chronić swoje konto.</p>
        </div>
        <div class="col-lg-4 text-end d-none d-lg-block">
            <i class="bi bi-shield-lock-fill text-warning" style="font-size: 5rem; opacity: 0.6;"></i>
        </div>
    </div>
</section>

<div class="row justify-content-center">
    <div class="col-lg-6">
        <div class="card border-0 shadow-sm rounded-4 overflow-hidden">
            <div class="card-body p-4 p-lg-5">
                <form asp-action="ChangePassword" method="post">
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger rounded-3 mb-3 small" role="alert"></div>

                    <div class="mb-4">
                        <label asp-for="OldPassword" class="form-label fw-bold text-secondary text-uppercase small letter-spacing-1">Aktualne hasło</label>
                        <div class="input-group">
                            <span class="input-group-text bg-light border-0 rounded-start-pill ps-3"><i class="bi bi-lock text-muted"></i></span>
                            <input asp-for="OldPassword" class="form-control form-control-lg border-0 bg-light rounded-end-pill shadow-none" placeholder="••••••••" />
                        </div>
                        <span asp-validation-for="OldPassword" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="NewPassword" class="form-label fw-bold text-secondary text-uppercase small letter-spacing-1">Nowe hasło</label>
                        <div class="input-group">
                            <span class="input-group-text bg-light border-0 rounded-start-pill ps-3"><i class="bi bi-lock-fill text-muted"></i></span>
                            <input asp-for="NewPassword" class="form-control form-control-lg border-0 bg-light rounded-end-pill shadow-none" placeholder="••••••••" />
                        </div>
                        <span asp-validation-for="NewPassword" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="mb-5">
                        <label asp-for="ConfirmPassword" class="form-label fw-bold text-secondary text-uppercase small letter-spacing-1">Powtórz nowe hasło</label>
                        <div class="input-group">
                            <span class="input-group-text bg-light border-0 rounded-start-pill ps-3"><i class="bi bi-shield-check text-muted"></i></span>
                            <input asp-for="ConfirmPassword" class="form-control form-control-lg border-0 bg-light rounded-end-pill shadow-none" placeholder="••••••••" />
                        </div>
                        <span asp-validation-for="ConfirmPassword" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="d-flex gap-3">
                        <a asp-action="Index" class="btn btn-light btn-lg rounded-pill fw-semibold px-4">Anuluj</a>
                        <button type="submit" class="btn btn-gold btn-lg rounded-pill fw-bold flex-fill shadow-sm">
                            <i class="bi bi-check-lg me-2"></i>Zmień hasło
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
